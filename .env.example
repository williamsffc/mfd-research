# Web3Forms access key for the contact form (https://web3forms.com/).
# Copy to `.env` locally; set the same name in Cloudflare Pages → Settings → Environment variables.
PUBLIC_WEB3FORMS_ACCESS_KEY=your_access_key_here
