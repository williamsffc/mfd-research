{
  "name": "mfd-research",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 8080",
    "build": "astro build",
    "preview": "astro preview --port 8080",
    "verify:parity": "node scripts/verify-legacy-parity.mjs",
    "test": "npm run build && npm run verify:parity && npm run test:a11y",
    "test:a11y": "node scripts/test-a11y.mjs",
    "optimize:images": "node scripts/optimize-images.mjs"
  },
  "devDependencies": {
    "astro": "^5.13.0",
    "axe-core": "^4.10.2",
    "sharp": "^0.34.5"
  }
}
